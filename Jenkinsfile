pipeline {
    agent any
    
    stages {
        stage('run 1py ev 1min Stage') {
            steps {
                // Execute a single shell command
                sh 'python3 1.py'
                
            }
        }
    }
}
